#version 460
#extension GL_EXT_buffer_reference2 : require
#extension GL_EXT_scalar_block_layout : require
#extension GL_EXT_shader_explicit_arithmetic_types_int64 : require

#include "../common/Vulkan/VK_push_constants.glsl"

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(push_constant, scalar) uniform PushConstants {
    PushConstantsDDGIProbeIrradianceDispatchArgs data;
} pc;

layout(buffer_reference, scalar) readonly buffer ProbeIrradianceCounterBuffer { uint probeIrradianceCounter; };
layout(buffer_reference, scalar) buffer DispatchIndirectCommandBuffer { uint num_groups_x; uint num_groups_y; uint num_groups_z; };

void main() {
    ProbeIrradianceCounterBuffer counter = ProbeIrradianceCounterBuffer(pc.data.probeIrradianceCounterDeviceAddress);
    DispatchIndirectCommandBuffer command = DispatchIndirectCommandBuffer(pc.data.probeIrradianceDispatchArgsDeviceAddress);
    command.num_groups_x = counter.probeIrradianceCounter;
    command.num_groups_y = 1u;
    command.num_groups_z = 1u;
}
